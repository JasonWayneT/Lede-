# Change Request: CR-011 Fix Gmail service silent failures

## Metadata

- Change request ID: `CR-011`
- Type: bug fix
- Status: implemented
- Requested by: Jason Wayne
- Created: 2026-07-06
- Updated: 2026-07-06

## Request

Fix `BUG-010`: the ingest lookback-window parse in `_lookback_query` swallowed all errors silently
with no log line, and `get_credentials_path` raised a raw `FileNotFoundError` instead of a
`StageError`, reachable mid-OAuth-flow.

## Classification

- User-visible behavior change: minor — a corrupted `settings.json` now produces a log line
  (previously silent); a missing `credentials.json` now produces a consistent `StageError` message
  surfaced through the existing settings-route error handling instead of a raw exception
- Requires new requirement ID: no (hardens existing `FR-001`/`FR-002`)
- Requires design update: no
- Requires tests: yes
- Requires migration: no
- Risk level: low

## Impacted IDs

| ID | Impact type | Notes |
|---|---|---|
| `BUG-010` | add | Silent lookback-parse failure + raw `FileNotFoundError` from OAuth credential lookup |

## Documentation updates required before code

- [x] Requirements registry
- [x] Known bug record
- [x] Traceability matrix

## Proposed implementation tasks

| Task ID | Requirement IDs | Description | Status |
|---|---|---|---|
| `TASK-053` | `BUG-010` | Log a warning on lookback-settings parse failure instead of silently swallowing it | done |
| `TASK-054` | `BUG-010` | Wrap `get_credentials_path` failures in `build_auth_url`/`exchange_code` as `StageError` | done |
| `TASK-055` | `BUG-010` | Update `api/settings.py` OAuth routes to catch `StageError`; add regression tests | done |

## Verification

| Test ID | What it proves | Result |
|---|---|---|
| `test_build_auth_url_raises_stage_error_when_credentials_missing` | Missing `credentials.json` surfaces as `StageError(retryable=False)`, not a raw exception | passing |
| `test_exchange_code_raises_stage_error_when_credentials_missing` | Same, for `exchange_code` | passing |
| `test_lookback_query_logs_warning_on_corrupt_settings` | Corrupted `settings.json` logs a `WARNING` and still returns a usable default query | passing |
| `test_lookback_query_uses_configured_days_when_settings_valid` | A valid custom lookback value is actually applied | passing |

## Final notes

- Implementation summary: `app/services/gmail.py` now has a module logger; `_lookback_query` logs
  on parse failure; `build_auth_url`/`exchange_code` re-raise `get_credentials_path` failures as
  `StageError`; `api/settings.py`'s `authorize_redirect`/`reauthorize_gmail` updated to match.
- Tests run: `uv run pytest tests/services/test_gmail.py -q` (see task 10 of this audit pass for
  full-suite confirmation).
- Open risk: none identified.
