# Change Request: CR-012 Wire up missed-run UI banner

## Metadata

- Change request ID: `CR-012`
- Type: bug fix
- Status: implemented
- Requested by: Jason Wayne
- Created: 2026-07-06
- Updated: 2026-07-06

## Request

Fix `BUG-011`: `GET /api/briefings/missed` was a hardcoded stub; the dashboard never surfaced a
missed-run banner despite Story 9.3 AC-1 requiring one. Wire the real detection state through to
the API and the dashboard.

## Classification

- User-visible behavior change: yes — a real banner now appears when a scheduled run was missed
- Requires new requirement ID: no (fixes existing `FR-026`/Story 9.3 AC-1)
- Requires design update: no
- Requires tests: yes
- Requires migration: no
- Risk level: low

## Impacted IDs

| ID | Impact type | Notes |
|---|---|---|
| `BUG-011` | add | Missed-run retry had no user-visible signal |

## Documentation updates required before code

- [x] Requirements registry
- [x] Known bug record
- [x] Traceability matrix

## Proposed implementation tasks

| Task ID | Requirement IDs | Description | Status |
|---|---|---|---|
| `TASK-056` | `BUG-011` | Implement `GET /api/briefings/missed` against real `check_missed_runs` state + active-run check | done |
| `TASK-057` | `BUG-011` | Compute `missed_run` context in `main.py`'s dashboard route; render banner in `dashboard.html` | done |
| `TASK-058` | `BUG-011` | Add regression tests for the endpoint and the dashboard banner | done |

## Verification

| Test ID | What it proves | Result |
|---|---|---|
| `test_missed_returns_none_when_no_schedule` | No missed run → `missed_at: None`, `retrying: False` | passing |
| `test_missed_reports_retrying_when_a_run_is_active` | A missed run with an active Run reports `retrying: True` | passing |
| `test_missed_reports_not_retrying_when_no_active_run` | A missed run with no active Run reports `retrying: False` | passing |
| `test_dashboard_shows_missed_run_banner` | Banner renders on the dashboard when a run was missed | passing |
| `test_dashboard_no_missed_run_banner_when_none_missed` | No banner when nothing was missed | passing |
| `test_dashboard_missed_run_banner_shows_retrying_when_run_active` | Banner text reflects an active retry | passing |

## Final notes

- Implementation summary: `GET /api/briefings/missed` now calls the same `check_missed_runs`
  function used for the startup retry, plus a DB check for an active Run; `main.py`'s dashboard
  route computes the same state server-side (with a portable time format) and `dashboard.html`
  renders a banner alongside the existing `BUG-006` hold banner.
- Tests run: `uv run pytest tests/api/test_briefings.py tests/api/test_dashboard.py -q` (see task
  10 of this audit pass for full-suite confirmation).
- Open risk: none identified. The banner is derived state (not cached), so it stays correct across
  both the web-server and daemon retry paths without needing cross-process state sharing.
