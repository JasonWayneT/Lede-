# Change Request: CR-008 Backfill Epic 13 specs (on-demand YouTube/article ingest)

## Metadata

- Change request ID: `CR-008`
- Type: documentation (retroactive — code already shipped ahead of spec)
- Status: implemented
- Requested by: Jason Wayne
- Created: 2026-07-06
- Updated: 2026-07-06

## Request

An audit pass (`AUDIT_LOG.md`, findings P3 / audit-agent confirmation) found that PRD FR-26
(YouTube transcript ingest), FR-27 (article URL ingest), and FR-28 (on-demand ingest UI/endpoint) —
`docs/epics-stories.md` Epic 13 — are fully implemented, wired into the orchestrator, and covered
by real tests (`tests/services/test_youtube.py`, `test_article.py`, `tests/pipeline/test_ordering.py`),
but have zero entries in `docs/spec/02-requirements-registry.md`, zero rows in
`docs/spec/06-traceability/traceability-matrix.md`, and no `docs/spec/03-feature-specs/13-*.md`
files at all. This CR backfills the spec layer to describe what the code actually does, closing the
gap against this project's own "specs before code" rule. No code changes.

## Classification

- User-visible behavior change: no (already shipped)
- Requires new requirement ID: yes (`FR-033`, `FR-034`, `FR-035`)
- Requires design update: no
- Requires tests: no (existing tests already cover the behavior described)
- Requires migration: no
- Risk level: low (paperwork only)

## Impacted IDs

| ID | Impact type | Notes |
|---|---|---|
| `FR-033` | add | YouTube transcript ingest — extracts a transcript per URL via `youtube_transcript_api`, skips URLs with no transcript or under 100 words |
| `FR-034` | add | Article URL ingest — extracts body text via `trafilatura`, falls back to Jina Reader (`r.jina.ai`) when trafilatura returns under 200 words |
| `FR-035` | add | On-demand ingest endpoint/UI — `POST /api/briefings/on-demand` accepts a URL list + source type, extracts synchronously, then runs the same pipeline from `embed` onward via a dedicated `run_pipeline_on_demand` orchestration path |
| `AC-092..AC-095` | add | Regression ACs for FR-033/034/035 |

## Documentation updates required before code

- [x] Requirements registry
- [x] Feature specs (`13-1-youtube-transcript-ingest.md`, `13-2-article-url-ingest.md`, `13-3-on-demand-ingest-ui-and-api.md`)
- [x] Traceability matrix
- [ ] Design spec / ADR — not needed; on-demand path is described in `ADR-004` already (briefly)
- [ ] Known bug record — not applicable, not a bug (separate SSRF finding tracked under `CR-009`/`BUG-008`)

## Proposed implementation tasks

| Task ID | Requirement IDs | Description | Status |
|---|---|---|---|
| `TASK-044` | `FR-033` | Write `13-1-youtube-transcript-ingest.md` describing `services/youtube.py` behavior | done |
| `TASK-045` | `FR-034` | Write `13-2-article-url-ingest.md` describing `services/article.py` behavior | done |
| `TASK-046` | `FR-035` | Write `13-3-on-demand-ingest-ui-and-api.md` describing the endpoint + `run_pipeline_on_demand` | done |
| `TASK-047` | `FR-033`, `FR-034`, `FR-035` | Add registry entries and traceability rows | done |

## Verification

| Test ID | What it proves | Result |
|---|---|---|
| `tests/services/test_youtube.py` (existing) | Video-ID extraction, transcript fetch, min-word skip, batch behavior | passing (pre-existing) |
| `tests/services/test_article.py` (existing) | trafilatura extraction, Jina fallback, min-word thresholds | passing (pre-existing) |
| `tests/pipeline/test_ordering.py` (existing) | Section/story ordering shared between assemble and tts_prep | passing (pre-existing) |

## Final notes

- Implementation summary: no code changed. Three feature specs written to describe existing,
  tested behavior; registry and traceability matrix updated.
- Tests run: none needed fresh — existing suite already covers this behavior; full suite re-run
  happens at the end of this audit pass.
- Open risk / follow-up: the SSRF exposure in `services/article.py` (fetching arbitrary
  user-supplied URLs server-side with no host/scheme guard) is tracked and fixed separately under
  `CR-009`/`BUG-008` — this CR is documentation-only and does not address it.
