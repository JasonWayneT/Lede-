# Change Request: CR-002 Fix pre-existing test debt from Form-encoded settings refactor, OAuth redirect-flow refactor, and a stale config default assertion

## Metadata

- Change request ID: `CR-002`
- Type: bug fix
- Status: implemented
- Requested by: Jason Wayne
- Created: 2026-07-04
- Updated: 2026-07-04

## Request

`uv run pytest -q` on `master` (before the `num_ctx`/condense work in `CR-001`) shows 6
pre-existing, unrelated failures: 4 in `tests/api/test_settings.py`, 1 in
`tests/services/test_gmail.py`, and 1 in `tests/test_config.py`. Investigate each failure's root
cause and fix or update accordingly, per `AGENTS.md`'s bug-fix workflow.

Investigation found all 6 are stale test expectations, not application regressions: three
separate feature commits made intentional, correct changes to application code
(`1fc854c`, `cfce0a9`, `7281a94` — Form-encoded settings routes to match the real htmx UI;
`6e0a3dd` — OAuth redirect flow to replace a blocking local-server flow that can't run inside a
web server; `b2626a4` — Ollama default model upgrade) and none of them updated the corresponding
tests or, in two cases, the feature specs describing that behavior.

## Classification

- User-visible behavior change: no — application code is unchanged
- Requires new requirement ID: no — new `BUG-*`/`AC-*`/`TEST-*` IDs only
- Requires design update: no
- Requires tests: yes
- Requires migration: no
- Risk level: low

## Impacted IDs

| ID | Impact type | Notes |
|---|---|---|
| `BUG-002` | add | Settings PUT tests send JSON to Form-encoded routes (gmail label, schedule cadence, tts engine/CUDA) |
| `BUG-003` | add | Gmail OAuth test targets `authorize()`/`InstalledAppFlow`, removed by the redirect-flow refactor |
| `BUG-004` | add | `test_app_config_defaults` asserts a stale `ollama_model_name` default |
| `AC-061..064` | add | Regression ACs for `BUG-002` |
| `AC-065` | add | Regression AC for `BUG-003` |
| `AC-066` | add | Regression AC for `BUG-004` |
| `TEST-012` | add | Settings API form-encoded PUT contract (replaces informal coverage from Story 8.4/8.6/8.7 task checklists) |
| `TEST-013` | add | Gmail OAuth redirect flow (`build_auth_url` / `exchange_code`) |
| `TEST-002` | update | Corrected `ollama_model_name` default assertion |
| `FR-001` | note | Feature spec `2-1-gmail-oauth-authorization.md` corrected to describe the redirect flow instead of the removed `InstalledAppFlow` design |
| `FR-022`, `FR-026`, `FR-012` | note | Feature specs `8-4`, `8-7`, `6-3` corrected to describe the Form-encoded PUT contract instead of the original JSON-body design |

## Documentation updates required before code

- [x] Requirements registry
- [x] Feature spec
- [x] Traceability matrix
- [x] Test spec
- [x] Known bug record
- [ ] Design spec — not applicable, no design change
- [ ] ADR — not applicable, no architecture change

## Proposed implementation tasks

| Task ID | Requirement IDs | Description | Status |
|---|---|---|---|
| `TASK-015` | `BUG-002`, `AC-061..064` | Fix `tests/api/test_settings.py` to send form-encoded (`data=`) bodies for gmail/schedule/tts PUT routes; also correct `test_put_tts_kokoro_succeeds`, which shared the defect masked by a default-value coincidence | done |
| `TASK-016` | `BUG-003`, `AC-065` | Replace `test_authorize_stores_token_via_credentials_set` in `tests/services/test_gmail.py` with tests covering `build_auth_url()` and `exchange_code()` | done |
| `TASK-017` | `BUG-004`, `AC-066` | Fix `tests/test_config.py::test_app_config_defaults` Ollama model assertion | done |
| `TASK-018` | `FR-001`, `FR-022`, `FR-026`, `FR-012` | Correct stale feature-spec descriptions in `2-1-gmail-oauth-authorization.md`, `8-4-settings-gmail-configuration.md`, `8-7-settings-schedule-and-daemon.md`, `6-3-tts-engine-settings.md` | done |

## Verification

| Test ID | What it proves | Result |
|---|---|---|
| `TEST-012` | Settings PUT routes for gmail/schedule/tts behave correctly under form-encoded requests (the real UI's request shape) | passing |
| `TEST-013` | OAuth redirect flow builds a consent URL and stores the exchanged token via `credential_store.set` | passing |
| `TEST-002` | `AppConfig` defaults match shipped configuration | passing |

## Final notes

- Implementation summary: no application code changed. Fixed 6 stale tests across
  `tests/api/test_settings.py` (4), `tests/services/test_gmail.py` (1), `tests/test_config.py` (1),
  and corrected 4 feature specs whose prose still described designs that were intentionally
  replaced by earlier feature commits.
- Tests run: `uv run pytest -q` (full suite) — see verification table above.
- Open risks: none. `_pending_verifiers` (module-level dict keyed by OAuth `state`) is
  single-process/single-user by design per its existing comment in `gmail.py` — unchanged by this CR.
- Follow-ups: none required to close this CR.
